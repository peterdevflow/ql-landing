/**
 * Site configuration.
 *
 * Change `mode` to "live" when the app is ready and you want
 * CTA buttons to link directly to the product / payment page.
 */
export const siteConfig = {
  mode: "waitlist" as "waitlist" | "live",
} as const;
